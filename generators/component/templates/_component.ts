import {Component} from '@angular/core';

@Component({
   selector: '<%= componentName %>',
   templateUrl: './<%= name %>.html',
   styleUrls: ['./<%= name %>.scss'],
})
export class <%= className %> {
 
}