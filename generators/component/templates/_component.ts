import {Component} from '@angular/core';

@Component({
   selector: '<%= selector %>',
   templateUrl: './<%= name %>.html',
   styleUrls: ['./<%= name %>.scss'],
})
export class <%= className %> {
 
}